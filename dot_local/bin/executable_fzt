#!/usr/bin/env bash

dir="$HOME/.config/alacritty/themes"

theme=$(fd -e toml -E theme.toml --base-directory "$dir" | fzf --info=inline --height=50% --layout=reverse)

if [[ -n "$theme" ]]; then
  ln -sf "$dir/$theme" "$dir/theme.toml"
fi
